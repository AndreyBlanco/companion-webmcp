# Required only for local or Netlify semantic extraction. Never commit real values.
OPENAI_API_KEY=replace-with-a-project-key
COMPANION_DEMO_ACCESS_CODE=replace-with-a-private-judge-code

# Optional; the single supported semantic provider defaults to gpt-5-mini.
COMPANION_SEMANTIC_MODEL=gpt-5-mini
